"""psd_html: PSD -> OFT email pipeline (dump/analyze/build/validate/route/linkgen/emit/bakeoff/preview/gate/compare -- see cli.py): solve a PSD into a table-based IR, emit an OFT-safe HTML bundle, then gate and compare it for fidelity."""

__version__ = "0.1.0"
